import { Request, Response, Router } from 'express';
// Removed Supabase import
// import { auth, db } from '../config/supabase.config';

const router = Router();

// Define your routes here without Supabase integration

export default router;
